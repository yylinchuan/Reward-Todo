# 可复制给朋友 AI 的安装任务

请帮我把这个仓库中的“小票待办”接入我的 CyberBoss/AI 工作区。

你必须先完整阅读仓库根目录的 `AI-INSTALL.md`，然后遵守以下要求：

1. 先检查 Node.js、仓库目录、独立前端构建、AI 工作区目录、CyberBoss 目录和当前 `.mcp.json`，不要先写入。
2. 先执行安装脚本的 `Plan` 模式，把完整预演结果和能力边界解释给我。
3. 没有得到我在当前对话中的明确同意，不得执行 `Apply`。
4. 不得复制仓库作者、其他用户或旧设备的 SQLite、备份、`.env`、聊天记录、密钥和个人数据；为我创建空白数据空间。
5. 不覆盖已有 MCP 配置，不设置开机自启动，不自动重启或停止 CyberBoss，不改模型、账号、Git 远程和公网设置。
6. 询问我希望在小票中显示的用户名字与 AI 名字，以及是否创建“Reward-Todo”桌面快捷方式。快捷方式只能由我点击启动，不得设置 Windows 登录自启动。
7. 安装后运行 `doctor.ps1`，再只读调用 `reward_todo_manage` 的 `status`、`list`、`calendar` 做验收。不要创建测试待办污染我的真实数据。
8. 清楚区分：能由 AI 主动增删改查，与 21:30 提醒能否真正送进既有聊天线程。若 CyberBoss 主动提醒适配未安装，只报告缺口，不要向未知版本源码硬打补丁。
9. 最后告诉我：改了哪些文件、数据存在哪里、网页怎样打开、如何停用/卸载、是否需要我自行重启工作区。不要替我重启。
